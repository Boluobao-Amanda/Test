# PETS LOVE - 生产环境配置模板
# 复制此文件为 .env.production.local 并填入实际值

# =============================================================================
# 数据库配置
# =============================================================================
# 生产环境建议升级到 Vercel Postgres 或 PlanetScale
DATABASE_URL="file:./prod.db"

# =============================================================================
# Stripe 生产环境配置 (必填)
# =============================================================================
# 从 Stripe Dashboard -> Developers -> API keys 获取
STRIPE_SECRET_KEY="sk_live_..."
STRIPE_PUBLISHABLE_KEY="pk_live_..."

# 从 Stripe Dashboard -> Developers -> Webhooks 获取
# Webhook 端点: https://your-domain.vercel.app/api/webhooks/stripe
# 监听事件: payment_intent.succeeded, payment_intent.payment_failed
STRIPE_WEBHOOK_SECRET="whsec_..."

# =============================================================================
# 应用安全配置 (必填)
# =============================================================================
# 生产环境域名
NEXTAUTH_URL="https://your-domain.vercel.app"

# 生成强随机密钥: openssl rand -base64 32
NEXTAUTH_SECRET="your-production-secret-key-here"

# =============================================================================
# 邮件服务配置 (可选，用于订单通知)
# =============================================================================
# 推荐使用 SendGrid, Mailgun, 或 AWS SES
SMTP_HOST="smtp.sendgrid.net"
SMTP_PORT=587
SMTP_USER="apikey"
SMTP_PASS="your-sendgrid-api-key"

# =============================================================================
# 文件上传配置
# =============================================================================
# 文件大小限制 (字节)
UPLOAD_MAX_SIZE=10485760

# 生产环境建议使用 Vercel Blob 或 AWS S3
UPLOAD_DIR="./uploads"

# =============================================================================
# 监控和分析 (可选)
# =============================================================================
# Google Analytics
# NEXT_PUBLIC_GA_ID="G-XXXXXXXXXX"

# Sentry 错误监控
# SENTRY_DSN="https://xxx@sentry.io/xxx"